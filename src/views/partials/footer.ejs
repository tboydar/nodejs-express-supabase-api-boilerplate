<footer class="footer bg-dark text-light mt-5">
    <div class="container py-5">
        <div class="row">
            <!-- Company Info -->
            <div class="col-lg-4 col-md-6 mb-4">
                <h5 class="fw-bold mb-3">
                    <i class="bi bi-shop"></i>
                    <%= __('site.title') %>
                </h5>
                <p class="text-light-emphasis">
                    <%= __('site.description') %>
                </p>
                <div class="social-links">
                    <a href="#" class="text-light me-3" title="Facebook">
                        <i class="bi bi-facebook fs-5"></i>
                    </a>
                    <a href="#" class="text-light me-3" title="Twitter">
                        <i class="bi bi-twitter fs-5"></i>
                    </a>
                    <a href="#" class="text-light me-3" title="Instagram">
                        <i class="bi bi-instagram fs-5"></i>
                    </a>
                    <a href="#" class="text-light" title="LinkedIn">
                        <i class="bi bi-linkedin fs-5"></i>
                    </a>
                </div>
            </div>
            
            <!-- Quick Links -->
            <div class="col-lg-2 col-md-6 mb-4">
                <h6 class="fw-bold mb-3"><%= __('navigation.products') %></h6>
                <ul class="list-unstyled">
                    <li class="mb-2">
                        <a href="<%= getLocalizedUrl('/products') %>" class="text-light-emphasis text-decoration-none">
                            <%= __('categories.all') %>
                        </a>
                    </li>
                    <li class="mb-2">
                        <a href="<%= getLocalizedUrl('/categories/electronics') %>" class="text-light-emphasis text-decoration-none">
                            <%= __('categories.electronics') %>
                        </a>
                    </li>
                    <li class="mb-2">
                        <a href="<%= getLocalizedUrl('/categories/clothing') %>" class="text-light-emphasis text-decoration-none">
                            <%= __('categories.clothing') %>
                        </a>
                    </li>
                    <li class="mb-2">
                        <a href="<%= getLocalizedUrl('/categories/books') %>" class="text-light-emphasis text-decoration-none">
                            <%= __('categories.books') %>
                        </a>
                    </li>
                </ul>
            </div>
            
            <!-- Customer Service -->
            <div class="col-lg-3 col-md-6 mb-4">
                <h6 class="fw-bold mb-3">Customer Service</h6>
                <ul class="list-unstyled">
                    <li class="mb-2">
                        <a href="<%= getLocalizedUrl('/help/contact') %>" class="text-light-emphasis text-decoration-none">
                            Contact Us
                        </a>
                    </li>
                    <li class="mb-2">
                        <a href="<%= getLocalizedUrl('/help/shipping') %>" class="text-light-emphasis text-decoration-none">
                            Shipping Info
                        </a>
                    </li>
                    <li class="mb-2">
                        <a href="<%= getLocalizedUrl('/help/returns') %>" class="text-light-emphasis text-decoration-none">
                            Returns
                        </a>
                    </li>
                    <li class="mb-2">
                        <a href="<%= getLocalizedUrl('/help/faq') %>" class="text-light-emphasis text-decoration-none">
                            FAQ
                        </a>
                    </li>
                </ul>
            </div>
            
            <!-- Languages -->
            <div class="col-lg-3 col-md-6 mb-4">
                <h6 class="fw-bold mb-3"><%= __('language.switch') %></h6>
                <div class="d-flex flex-column gap-2">
                    <% supportedLanguages.forEach(lang => { %>
                        <% 
                            const isActive = lang.code === locale;
                            const currentPath = req.path.replace(/^\/[a-z]{2}(-[A-Z]{2})?/, '') || '/';
                            const langUrl = getLocalizedUrl(currentPath, lang.code);
                        %>
                        <a href="<%= langUrl %>" 
                           class="text-light-emphasis text-decoration-none d-flex align-items-center <%= isActive ? 'fw-bold' : '' %>">
                            <span class="me-2">
                                <% if (lang.code === 'en') { %>🇺🇸<% } %>
                                <% if (lang.code === 'zh-TW') { %>🇹🇼<% } %>
                                <% if (lang.code === 'de') { %>🇩🇪<% } %>
                            </span>
                            <%= lang.nativeName %>
                            <% if (isActive) { %>
                                <i class="bi bi-check-circle-fill ms-auto text-success"></i>
                            <% } %>
                        </a>
                    <% }) %>
                </div>
            </div>
        </div>
        
        <hr class="my-4">
        
        <!-- Copyright -->
        <div class="row align-items-center">
            <div class="col-md-6">
                <p class="mb-0 text-light-emphasis">
                    &copy; <%= new Date().getFullYear() %> <%= __('site.title') %>. All rights reserved.
                </p>
            </div>
            <div class="col-md-6 text-md-end">
                <small class="text-light-emphasis">
                    Built with Node.js + Express + Drizzle ORM
                </small>
            </div>
        </div>
    </div>
</footer>